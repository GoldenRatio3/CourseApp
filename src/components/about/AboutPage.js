import React from "react";

const AboutPage = () => (
  <div>
    <h2>About</h2>
    <p>
      THis app uses React, Redux and React Router, plus many other helpful
      libraries.
    </p>
  </div>
);

export default AboutPage;
